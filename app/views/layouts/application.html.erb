<!DOCTYPE html>
<html>
  <head>
    <title>Budget</title>
    <%= csrf_meta_tags %>
    <link href="https://fonts.googleapis.com/css?family=Diplomata+SC|Open+Sans" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= favicon_link_tag 'http://i.imgur.com/9LMGF81.png' %>

  </head>

  <body>
    <div id="container">
    <div id= "header">
      <h1>Budget Log</h1>
      </div>
      <% if current_user %>
        <div id="nav">
          <ul>
            <li><a href = "/dashboard">Home</a></li>
            <li><a href = "/assets">Assets</a></li>
            <li><a href = "/expenses">Expenses</a></li>
            <li style="float:right">
              <% end %>
              <div id="login">
              <% if !current_user %>
                <%= link_to "Sign up", new_user_registration_path %>
                <%= link_to "Sign in", new_user_session_path %>
              <% else %>
                <%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
              <% end %>
            </li>
          </ul>
        </div>
      </div>
      <% if current_user %>
      <div id="balance">
      <!-- Total Assets <%= number_to_currency(@total_assets) %>
      Total Expenses <%= number_to_currency(@total_expenses) %> -->
      <h2>Balance <%= number_to_currency(@net_total) %>
    </h2>
      <% end %>
    </div>



    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
    <footer>
      <p> &copy; Mitchell Carroll, 2017</p>
    </footer>
  </body>
</html>
